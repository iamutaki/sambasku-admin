# Staging (pnpm build:staging / vite build --mode staging).
VITE_API_BASE_URL=https://sambasku-staging.iamutaki.com/api/v1
VITE_APP_NAME=Kamus Sambas - Admin Console (Staging)

# CATATAN: kredensial ImageKit TIDAK diperlukan di frontend - alur upload
# server-driven (public_key/signature/token datang dari API saat runtime,
# endpoint /admin/images/upload-token). Tempatnya hanya di API:
# api/.env.staging (lokal) + wrangler secret (Workers staging).
